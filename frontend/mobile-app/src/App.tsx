import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';

export default function App() {
  return (
    <SafeAreaView>
      <View>
        <Text>RoomieMatch Mobile</Text>
        <Text>Matching, chat, and profile modules are ready to build.</Text>
      </View>
    </SafeAreaView>
  );
}
